---
layout: section
---

# 4.2 · Generics

---
layout: center
---

## El problema: repetir por cada tipo

Sin genéricos habría que escribir la misma función una vez por tipo:

```rust
fn mayor_i32(a: i32, b: i32) -> i32 { if a > b { a } else { b } }
fn mayor_f64(a: f64, b: f64) -> f64 { if a > b { a } else { b } }
```

<br>

Un **genérico** abstrae el tipo con un parámetro (`T`), y el compilador genera la versión concreta para cada uso:

```rust
fn mayor<T: PartialOrd>(a: T, b: T) -> T {
    if a > b { a } else { b }
}
```

---
layout: center
---

## Trait bounds: donde se juntan traits y generics

`<T: PartialOrd>` es un ***trait bound***: no acepta *cualquier* tipo, sino cualquiera **que cumpla ese trait**.

<div class="important-note">

Se lee: *"`T` es cualquier tipo, **siempre que** se pueda comparar con `>`"*. Sin el bound el compilador rechaza `a > b`, porque no todo tipo sabe compararse.

</div>

<div class="grid grid-cols-2 gap-x-6 mt-4 w-full text-left">
  <div>

```rust
// un bound
fn mayor<T: PartialOrd>(a: T, b: T) -> T

// varios bounds
fn mostrar<T: Debug + Clone>(x: T)
```

  </div>

<div>

```rust
// sintaxis `where`: equivalente, y
// más legible con muchos bounds
fn procesar<T, U>(a: T, b: U)
where
    T: Debug + Clone,
    U: PartialOrd,
{ }
```

</div>

</div>

---
layout: center
---

## Structs genéricos

El parámetro de tipo también sirve para definir estructuras que funcionan con cualquier contenido:

```rust
struct Par<T> {
    primero: T,
    segundo: T,
}

impl<T: PartialOrd> Par<T> {
    fn mayor(&self) -> &T {
        if self.primero > self.segundo { &self.primero }
        else { &self.segundo }
    }
}
```

* `struct Par<T>` declara el parámetro; `impl<T: ...> Par<T>` lo vuelve a declarar para el bloque de métodos.
* El bound puede vivir solo en el `impl`: así el struct admite cualquier `T`, pero `mayor()` existe únicamente si `T` es comparable.

---
layout: center
---

## Tipos asociados

Un parámetro `<T>` lo elige quien llama. Hay contratos donde el tipo lo fija la
implementación: el `trait` lo declara sin valor y cada `impl` lo resuelve.

```rust
trait Contenedor {
    type Elemento;                 // parte del contrato, sin valor aún
    fn primero(&self) -> Option<&Self::Elemento>;
}

struct Bolsa { datos: Vec<u32> }

impl Contenedor for Bolsa {
    type Elemento = u32;                  // la implementación lo fija
    fn primero(&self) -> Option<&u32> { self.datos.first() }
}
```

---
layout: center
---

## Parámetro o tipo asociado

<div class="important-note">

Criterio: `<T>` admite varias implementaciones por tipo, `Trait<u32>` y
`Trait<String>` a la vez. Un tipo asociado admite una sola. Se usa cuando el tipo es
consecuencia del `impl` y no una opción de quien llama.

</div>

En un bound se nombra con `=`, como en `<I: Iterator<Item = u64>>`. Los dos casos
más frecuentes son `Iterator::Item`, de 5.6 Iteradores, y `Future::Output`, de
9.2 async, await y Future.

---
layout: center
---

## `impl Trait` en el retorno

Como argumento, `impl Trait` es azúcar para un parámetro genérico y lo elige quien
llama. **Como retorno significa lo contrario:** el tipo concreto lo elige la función
y no se escribe.

```rust
fn impares(desde: u32) -> impl Iterator<Item = u32> {
    (desde..).step_by(2)
}

fn main() {
    let v: Vec<u32> = impares(1).take(5).collect();
    println!("{v:?}");   // [1, 3, 5, 7, 9]
}
```

<div class="important-note">

El tipo real de ese iterador es `StepBy<RangeFrom<u32>>`, y con dos adaptadores más
se vuelve impronunciable. `impl Iterator` promete el trait y esconde el tipo, sin
costo en ejecución: sigue siendo despacho estático (6.3 Despacho dinámico).

</div>

---
layout: center
---

## Dónde vas a encontrarlos

Casi todo lo que viene en las secciones 5 y 6 es genérico:

<div class="text-sm cp-table-compact">

| Tipo | Parámetros | Bound que exige |
| :--- | :--- | :--- |
| `Vec<T>` | el tipo de los elementos | ninguno |
| `Option<T>` / `Result<T, E>` | el valor y el error | ninguno |
| `Box<T>` / `Rc<T>` / `RefCell<T>` | el dato apuntado | ninguno |
| `HashMap<K, V>` | llave y valor | `K: Hash + Eq` |
| `BinaryHeap<T>` | los elementos | `T: Ord` |

</div>

<div class="important-note">

**Costo cero.** El compilador genera una versión concreta por cada tipo usado
(*monomorfización*): un `Vec<i32>` compila al mismo código que una versión escrita a
mano para `i32`.

</div>

<div class="cp-ver-tambien">

Ver también · 4.1 Traits · 5.1 Option · 5.3 Vec

</div>
