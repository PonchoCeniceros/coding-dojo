---
layout: section
---

# Por qué Rust

---
layout: center
---

## Por qué Rust

<div class="important-note">

**Página pendiente.** Contenido previsto:

* Qué problema resuelve que C++ y Go no.
* La tesis: *aliasing o mutación, nunca las dos a la vez, verificado en compilación*.
* Ownership, `&`/`&mut`, lifetimes, `Send`/`Sync` y `RefCell` como consecuencias de esa frase.
* Sin GC, sin `null`, sin excepciones: qué ocupa su lugar.

</div>
